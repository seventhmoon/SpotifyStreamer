<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <CheckBoxPreference
        android:key="@string/key_notification"
        android:title="@string/pref_desc_media_style_notification"

        android:defaultValue="true" />



    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:key="@string/key_locale"
        android:title="@string/pref_desc_country_code"
        android:defaultValue="HK"
        android:entries="@array/pref_countryCodes"
        android:entryValues="@array/pref_countryCodes"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

</PreferenceScreen>
